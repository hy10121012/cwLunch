<nav class="row">
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header navbar-left">
        <div class="navbar-text">
          <a href="/items/new" class="btn btn-default">新增菜品</a>
        </div>
      </div>
      <div class="navbar-header navbar-right">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <div class="navbar-text navbar-left">
          <button type="submit" class="btn btn-primary">发送菜单信息</button>
        </div>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <%=form_for @setB_menu,:url=>{:action=>"update_setB_menu"}, :html=>{:class=>"navbar-form navbar-left", :role=>"plan_set", :style=>"margin-top: 15px;padding-left:0px"} do |setB_menu_form|%>
          <div class="form-group">
            修改B套餐配菜信息 <%=setB_menu_form.text_field :value, :class => "form-control", :placeholder => "请输入B套餐配菜信息",:style=>"width: 300px"%>
          </div>
          <button type="submit" class="btn btn-default">更新</button>
        <% end %>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->

    </div>
    <!-- /.container-fluid -->
  </nav>
</nav>


<div class="row">
  <table class="table table-hover table-striped">
    <tr>
      <th>中文菜名</th>
      <th>英文菜名</th>
      <th>价格A</th>
      <th>价格B</th>
      <th>今日菜单</th>
      <th>修改</th>
    </tr>
    <% for item in @items %>
        <tr>
          <td><%=item.name_cn%></td>
          <td><%=item.name_en%></td>
          <td><%=item.price_a%></td>
          <td><%=item.price_b%></td>
          <td><input <%if item.is_live==1%>checked='checked'<%end%> type="checkbox"></td>
          <td><a class="btn btn-primary" href="/items/<%=item.id%>/edit">修改</a></td>
        </tr>
    <%end%>
  </table>
</div>
